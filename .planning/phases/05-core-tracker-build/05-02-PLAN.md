# Plan 05-02: Run benchmark segment, validate player_ids.json output

**Phase:** 5 — Core Tracker Build
**Goal:** Run player_tracker.py on first 5 minutes, validate output schema, build player profile

## Tasks

<wave id="1" label="Benchmark segment run">
  <task id="1.1" type="shell">
    Run player_tracker.py on first 300 seconds
    Command: python3 ~/player_tracker.py \
      --video ~/veo-g15a-match-7mar2026/standard-1920x1080-followcam.mp4 \
      --player-jersey 4 \
      --end-time 300 \
      --output ~/veo-clips/player-continuity-tracking/player_ids.json
  </task>
</wave>

<wave id="2" label="Output validation">
  <task id="2.1" type="code">
    Write validate_output.py to check:
    - player_ids.json exists and is valid JSON
    - Frame coverage: frames annotated / total frames expected ≥ 90%
    - Per-annotation schema: frame_idx, track_id, bbox, jersey_ocr, cosine_sim present
    - At least 1 frame with target player detected
    Path: /home/agent/validate_tracker_output.py
  </task>
  <task id="2.2" type="shell">
    Run validate_tracker_output.py — must PASS
  </task>
</wave>

## Acceptance Criteria

1. player_ids.json exists with frame-level annotations covering the 5-minute segment
2. Output schema valid: frame_idx, track_id, bbox, jersey_ocr, cosine_sim in each annotation
3. At least 1 frame has a detection with jersey_ocr matching "4" or target player id assigned
