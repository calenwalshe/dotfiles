# Plan 05-01: Write player_tracker.py — frame-streaming loop (TDD)

**Phase:** 5 — Core Tracker Build
**Goal:** Build player_tracker.py with YOLOv8n → OSNet → EasyOCR → IoU tracker pipeline

## TDD Approach

Write failing tests first for critical modules, then implement to make them pass.

## Tasks

<wave id="1" label="TDD RED — failing tests">
  <task id="1.1" type="code">
    Write test_player_tracker.py with failing tests for:
    - letterbox/unletterbox round-trip
    - OSNet embedding normalization (unit vector)
    - Jersey OCR crop extraction (top 40% of bbox)
    - Cosine similarity thresholds (>0.7 match, >0.6 fallback)
    - Annotation output schema (required fields present)
    Path: /home/agent/test_player_tracker.py
  </task>
  <task id="1.2" type="shell">
    Run tests — all should FAIL (RED phase confirmed)
  </task>
</wave>

<wave id="2" label="Implementation — player_tracker.py">
  <task id="2.1" type="code">
    Write ~/player_tracker.py with:
    - ONNX Runtime YOLOv8n detection (reuse from benchmark)
    - ONNX Runtime OSNet embedding extraction
    - EasyOCR jersey number OCR (top 40% of person bbox)
    - Simple IoU tracker with cosine gallery (ByteTrack-style)
    - Player profile construction from first 30s reference frames
    - Per-frame annotation writer to player_ids.json
    - CLI args: --video, --player-jersey, --output, --end-time, --sample-rate
    - Test frame modes: --test-frame black|blur (resilience per eval plan)
    Path: /home/agent/player_tracker.py
  </task>
</wave>

<wave id="3" label="TDD GREEN — tests pass">
  <task id="3.1" type="shell">
    Run test_player_tracker.py — all tests must PASS
  </task>
</wave>

## Acceptance Criteria

1. test_player_tracker.py all tests GREEN
2. player_tracker.py runs on benchmark segment (--end-time 300) without crash
3. Produces player_ids.json with frame-level annotations
4. --test-frame black and --test-frame blur do not raise exceptions (resilience)
