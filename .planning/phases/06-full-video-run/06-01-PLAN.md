# Plan 06-01: Full-video run and tracklet gap-merge

**Phase:** 6 — Full-Video Run & Post-Processing
**Goal:** Run full 71-minute tracking pass; implement gap-merge; validate player_ids.json

## Tasks

<wave id="1" label="Start full-video run (background)">
  <task id="1.1" type="shell">
    Start full-video tracking run in background (will take ~6 hours)
    Command: nohup python3 ~/player_tracker.py \
      --video ~/veo-g15a-match-7mar2026/standard-1920x1080-followcam.mp4 \
      --player-jersey 4 \
      --output ~/veo-clips/player-continuity-tracking/player_ids_raw.json \
      --sample-rate 3 \
      > ~/veo-clips/player-continuity-tracking/tracker_full_run.log 2>&1 &
  </task>
</wave>

<wave id="2" label="Implement tracklet gap-merge">
  <task id="2.1" type="code">
    Write gap_merge.py: post-process tracker annotations to merge fragmented target player tracklets
    - Load player_ids.json (or player_ids_raw.json)
    - Find the target player track_id (highest cosine_sim or jersey_ocr match)
    - Identify frame ranges where target player appears
    - Merge consecutive ranges where gap < 5 seconds (< gap_frames at video fps)
    - Write final player_ids.json with merged annotations and target_player_frames list
    Path: /home/agent/gap_merge.py
  </task>
  <task id="2.2" type="shell">
    Test gap-merge on 20s benchmark output to validate logic
  </task>
</wave>

<wave id="3" label="Validate output (can run on partial output)">
  <task id="3.1" type="code">
    Write validate_tracker_output.py (if not already from Phase 5)
    Validates: schema, frame coverage, target player annotations present
  </task>
</wave>

## Acceptance Criteria

1. Full-video tracking run started (not required to complete before Phase 7 planning)
2. gap_merge.py merges tracklet gaps < 5s correctly (validated on test data)
3. Merged player_ids.json has frame-level annotations with gap_merged: true for merged segments
