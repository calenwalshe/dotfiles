---
phase: 03-alerting-and-operator-workflow
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - agent-stack/openclaw-scheduler/master_harness/suites/test_l2_perception_alerting.py
  - openclaw-fresh/workspace/tools/test_promote_bridge.py
autonomous: true
requirements: [INFR-02]

must_haves:
  truths:
    - "test_l2_perception_alerting.py exists with failing tests for bypass health and failure alert logic"
    - "test_promote_bridge.py exists with failing tests for list and promote operations"
    - "Both test files run without import errors using stdlib unittest"
  artifacts:
    - path: "agent-stack/openclaw-scheduler/master_harness/suites/test_l2_perception_alerting.py"
      provides: "RED test scaffold for alerting module"
      contains: "_compute_bypass_health"
    - path: "openclaw-fresh/workspace/tools/test_promote_bridge.py"
      provides: "RED test scaffold for promote_bridge.py"
      contains: "list_staging"
  key_links:
    - from: "test_l2_perception_alerting.py"
      to: "master_harness.suites.l2_perception_alerting"
      via: "import with skipIf guard"
      pattern: "skipIf.*ImportError"
    - from: "test_promote_bridge.py"
      to: "promote_bridge"
      via: "import with skipIf guard"
      pattern: "skipIf.*ImportError"
---

<objective>
Write failing unit test scaffolds for both Phase 3 subsystems before any implementation exists (TDD RED phase).

Purpose: Establish the test contracts that implementation must satisfy. Following the established skipIf import guard pattern from Phase 2, both test files load cleanly even when the target modules don't yet exist — tests are skipped in RED state, not erroring.
Output: Two test files with well-defined failure behaviors. Both test runners exit 0 with "skipped" output. No implementation yet.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-alerting-and-operator-workflow/03-RESEARCH.md
@.planning/phases/03-alerting-and-operator-workflow/03-VALIDATION.md

<interfaces>
<!-- Established patterns from Phase 2 — use these directly. -->

From master_harness/suites/test_l2_perception.py (Phase 2 import guard pattern):
```python
try:
    from master_harness.suites.l2_perception import (
        _prune_artifacts, _classify_screenshot, _artifact_dir, _detect_drift, _build_error_result,
    )
    _MODULE_MISSING = False
except ImportError:
    _MODULE_MISSING = True

@unittest.skipIf(_MODULE_MISSING, "l2_perception module not importable (Docker-only deps)")
class TestXxx(unittest.TestCase):
    ...
```

From research Phase 3 — target functions to test in alerting module:
- `_compute_bypass_health(pool, threshold)` → returns dict with total_challenged, bypassed, failed_bypass, success_rate, below_threshold
- `_get_consecutive_failures_for_site(pool, site_key)` → returns int
- `_send_failure_alert(site_key, url, prev_cls, curr_cls, consecutive)` — async, calls notifier
- `_detect_drift` already tested in Phase 2 — do NOT duplicate

From research Phase 3 — target functions to test in promote_bridge:
- `list_staging()` → {"ok": True, "staging": [...], "count": N}
- `promote_site(site_key)` → {"ok": True, "promoted": site_key, "base_url": url}
- Both use urllib.request — mock with unittest.mock.patch
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing alerting tests (RED)</name>
  <files>agent-stack/openclaw-scheduler/master_harness/suites/test_l2_perception_alerting.py</files>
  <behavior>
    - Test: _compute_bypass_health returns success_rate=1.0 and below_threshold=False when no rows challenged
    - Test: _compute_bypass_health returns correct success_rate when bypassed < total_challenged
    - Test: _compute_bypass_health sets below_threshold=True when rate < threshold AND total_challenged >= 3
    - Test: _compute_bypass_health sets below_threshold=False when total_challenged < 3 (sparse data suppression)
    - Test: _get_consecutive_failures_for_site returns 0 when most recent row passed=True
    - Test: _get_consecutive_failures_for_site returns 2 when 2 consecutive passed=False rows then passed=True
    - Test: failure alert trigger condition: drift_event with prev=PASS_CONTENT and curr=BLOCKED_CHALLENGE fires alert
    - Test: failure alert does NOT fire when prev was already non-PASS (already-known failure)
    - Test: failure alert does NOT fire when curr=PASS_CONTENT (recovery, not a new failure)
  </behavior>
  <action>
    Create `/home/agent/agent-stack/openclaw-scheduler/master_harness/suites/test_l2_perception_alerting.py`.

    Use the established skipIf import guard pattern (identical to test_l2_perception.py):
    ```python
    try:
        from master_harness.suites.l2_perception_alerting import (
            _compute_bypass_health, _get_consecutive_failures_for_site,
        )
        _MODULE_MISSING = False
    except ImportError:
        _MODULE_MISSING = True
    ```

    For `_compute_bypass_health` tests: pass a mock asyncpg pool using `unittest.mock.AsyncMock`. The function does a raw `pool.acquire()` context manager call — mock it to return a list of fake rows. Each row is a dict with `challenge_detected` (bool) and `passed` (bool). Test cases:
    - No challenged rows: success_rate=1.0, below_threshold=False
    - 3 challenged, 2 bypassed: success_rate=0.666..., below_threshold=True (threshold=0.5? no — 0.666 > 0.5 so False; use threshold=0.7 to test below)
    - 2 challenged (sparse, < 3): below_threshold=False regardless of rate
    - 3 challenged, 0 bypassed: success_rate=0.0, below_threshold=True with threshold=0.5

    For `_get_consecutive_failures_for_site` tests: mock the pool to return a list of rows with `passed` boolean. Test:
    - [True, False, False]: consecutive=0 (first row passed, no streak)
    - [False, False, True]: consecutive=2
    - [False, False, False]: consecutive=3
    - []: consecutive=0 (empty result)

    For alert trigger condition tests: these test the LOGIC of when to call `_send_failure_alert`, not the async send itself. Write a helper function `_should_alert(drift_event, curr_cls)` in the alerting module that returns bool — test this pure function. Cases:
    - drift_event={"prev": "PASS_CONTENT", "curr": "BLOCKED_CHALLENGE"}, curr_cls="BLOCKED_CHALLENGE" → True
    - drift_event=None → False (no drift)
    - drift_event={"prev": "BLOCKED_CHALLENGE", "curr": "SOFT_BLOCK"} → False (prev was already failing)
    - drift_event={"prev": "PASS_CONTENT", "curr": "PASS_CONTENT"} (same) → will never happen per _detect_drift logic but test returns False if curr==PASS_CONTENT

    Run tests: `cd /home/agent/agent-stack/openclaw-scheduler && python -m unittest master_harness.suites.test_l2_perception_alerting -v`
    Expected RED output: "skipped" (module not found) or ModuleNotFoundError is caught — NOT an unhandled crash.

    Commit atomically: `test(03-01): add failing tests for alerting module (RED)`
  </action>
  <verify>
    <automated>cd /home/agent/agent-stack/openclaw-scheduler && python -m unittest master_harness.suites.test_l2_perception_alerting -v 2>&1 | tail -5</automated>
  </verify>
  <done>Test file exists at correct path. Running tests exits 0. Output shows "skipped" or lists test names. No unhandled ImportError or SyntaxError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing promote_bridge tests (RED)</name>
  <files>openclaw-fresh/workspace/tools/test_promote_bridge.py</files>
  <behavior>
    - Test: list_staging calls GET /harness/sites?active_only=false and returns only active=False entries
    - Test: list_staging returns {"ok": False, "error": ...} when HTTP call fails
    - Test: promote_site calls GET list then POST /harness/sites with active=True
    - Test: promote_site returns error when site_key not found in staging
    - Test: promote_site returns {"ok": True, "promoted": site_key} on success
    - Test: promote_site with unknown site_key returns {"ok": False, "error": "...not found in staging queue"}
  </behavior>
  <action>
    Create `/home/agent/openclaw-fresh/workspace/tools/test_promote_bridge.py`.

    Use the established skipIf import guard pattern. Target module will be `promote_bridge` (co-located in same directory, import as `from promote_bridge import list_staging, promote_site`).

    For HTTP mocking: use `unittest.mock.patch("promote_bridge.urllib_request.urlopen")`. The mock must act as a context manager (use `MagicMock` with `__enter__` returning mock response that has `.read()` returning JSON bytes).

    Helper to make mock response:
    ```python
    def _mock_urlopen(response_data: dict):
        mock_resp = MagicMock()
        mock_resp.__enter__ = lambda s: s
        mock_resp.__exit__ = MagicMock(return_value=False)
        mock_resp.read.return_value = json.dumps(response_data).encode()
        return MagicMock(return_value=mock_resp)
    ```

    Test cases for `list_staging`:
    - Mock returns {"sites": [{"site_key": "s1", "active": False, "base_url": "http://a.com"}, {"site_key": "s2", "active": True, "base_url": "http://b.com"}]} → result["staging"] has 1 entry (s1 only), result["count"]==1
    - Mock raises urllib_error.URLError("connection refused") → result=={"ok": False, "error": ...}

    Test cases for `promote_site`:
    - Simulate two calls: first list returns staging=[{"site_key": "s1", "active": False, "base_url": "http://a.com", "category": "test", "priority": 100}], second POST returns {"ok": True} → promote_site("s1") returns {"ok": True, "promoted": "s1", "base_url": "http://a.com"}
    - Site not found: list returns empty staging → promote_site("unknown") returns {"ok": False, "error": "Site 'unknown' not found in staging queue"}

    Run tests: `cd /home/agent/openclaw-fresh/workspace/tools && python -m unittest test_promote_bridge -v`
    Expected RED: "skipped" (module not found). NOT a crash.

    Commit atomically: `test(03-01): add failing tests for promote_bridge (RED)`
  </action>
  <verify>
    <automated>cd /home/agent/openclaw-fresh/workspace/tools && python -m unittest test_promote_bridge -v 2>&1 | tail -5</automated>
  </verify>
  <done>Test file exists at /home/agent/openclaw-fresh/workspace/tools/test_promote_bridge.py. Running exits 0. Output shows skipped or test names. No unhandled crash.</done>
</task>

</tasks>

<verification>
Both test files produce exit code 0 when run. No unhandled ImportErrors. Test names are readable in output (confirming test discovery works).

Full scheduler test suite still passes (no regressions): `cd /home/agent/agent-stack/openclaw-scheduler && python -m unittest discover -s . -p "test_*.py" -v 2>&1 | tail -3`
</verification>

<success_criteria>
- test_l2_perception_alerting.py exists with 9+ test methods covering bypass health and alert trigger logic
- test_promote_bridge.py exists with 5+ test methods covering list and promote operations
- Both files use skipIf import guard pattern (matching Phase 2 precedent)
- Both test runners exit 0
- Existing Phase 2 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-alerting-and-operator-workflow/03-01-SUMMARY.md`
</output>
