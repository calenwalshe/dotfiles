---
phase: 03-alerting-and-operator-workflow
plan: "03"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - openclaw-fresh/workspace/tools/promote_bridge.py
  - openclaw-fresh/workspace/TOOLS.md
autonomous: true
requirements: [INFR-02]

must_haves:
  truths:
    - "Operator types /list-staging in Telegram and receives a list of staging queue URLs without editing config files"
    - "Operator types /promote <site_key> in Telegram and the URL moves to active test suite (active=True in site_test_catalog)"
    - "promote_bridge.py list correctly filters to active=False entries only"
    - "promote_bridge.py promote sends correct POST payload with active=True to scheduler API"
  artifacts:
    - path: "openclaw-fresh/workspace/tools/promote_bridge.py"
      provides: "CLI tool: list and promote staging URLs via scheduler HTTP API"
      exports: ["list_staging", "promote_site"]
    - path: "openclaw-fresh/workspace/TOOLS.md"
      provides: "Agent instruction for /list-staging and /promote commands"
      contains: "promote_bridge.py"
  key_links:
    - from: "TOOLS.md (main agent instruction)"
      to: "promote_bridge.py"
      via: "exec subprocess call pattern"
      pattern: "python3.*promote_bridge.py"
    - from: "promote_bridge.py"
      to: "http://openclaw-scheduler:9100/harness/sites"
      via: "urllib.request (same pattern as screenshot_agent_bridge.py)"
      pattern: "openclaw-scheduler:9100"
---

<objective>
Build the staging queue promotion tool and wire it into the main agent's TOOLS.md instruction file.

Purpose: Operator can list staging entries and promote URLs to active test suite via Telegram text commands — no config file editing required.
Output: promote_bridge.py (new tool in workspace/tools/), updated workspace/TOOLS.md with /list-staging and /promote command instructions.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-alerting-and-operator-workflow/03-RESEARCH.md
@.planning/phases/03-alerting-and-operator-workflow/03-01-SUMMARY.md

<interfaces>
<!-- Bridge pattern from Phase 1 — match this exactly. -->

From openclaw-fresh/workspace/tools/screenshot_agent_bridge.py (established bridge pattern):
```python
#!/usr/bin/env python3
"""Tool description.

Usage:
    python3 script.py <args>

Output: JSON result to stdout for agent consumption.
"""
import json
import sys
from urllib import request as urllib_request, error as urllib_error

SCHEDULER_URL = "http://openclaw-scheduler:9100"

def main():
    ...
    print(json.dumps(result))

if __name__ == "__main__":
    main()
```

From agent-stack/openclaw-scheduler/master_harness/api.py (live endpoints):
- GET  /harness/sites?active_only=false  → {"sites": [{site_key, base_url, active, category, priority, notes, ...}], "count": N}
- POST /harness/sites  body: {site_key, base_url, active, category, priority, notes}  → {"ok": true, "site": {...}}

From workspace/TOOLS.md (existing pattern — add new section):
The file already has sections for gcal, notes, github repos. Add a new section following the same style:
```markdown
## Perception Staging Queue

When the user asks to:
- "list staging URLs"
- "show what's in staging"
- "promote <site_key>"

Use promote_bridge.py:
- List: `python3 /var/lib/openclaw/workspace/tools/promote_bridge.py list`
- Promote: `python3 /var/lib/openclaw/workspace/tools/promote_bridge.py promote <site_key>`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build promote_bridge.py (GREEN)</name>
  <files>openclaw-fresh/workspace/tools/promote_bridge.py</files>
  <behavior>
    - list_staging() filters response to active=False entries only
    - list_staging() returns {"ok": False, "error": ...} on HTTP failure
    - promote_site(site_key) POSTs with active=True and preserves existing site fields
    - promote_site(site_key) returns error when site_key not in staging
    - promote_site(site_key) returns {"ok": True, "promoted": site_key, "base_url": ...} on success
    - main() dispatches "list" and "promote <site_key>" subcommands, printing JSON to stdout
  </behavior>
  <action>
    PRE-IMPLEMENTATION CHECK: Verify scheduler is reachable from openclaw-fresh container before coding:
    ```bash
    docker exec openclaw-fresh curl -s http://openclaw-scheduler:9100/health 2>&1 | head -5
    docker exec openclaw-fresh curl -s "http://openclaw-scheduler:9100/harness/sites?active_only=false" 2>&1 | head -5
    ```
    If hostname fails: check `docker inspect openclaw-fresh --format '{{range .NetworkSettings.Networks}}{{.NetworkID}}{{end}}'` and adjust SCHEDULER_URL accordingly. The correct hostname is documented in the research file.

    Create `/home/agent/openclaw-fresh/workspace/tools/promote_bridge.py` with the full implementation from the research file (Pattern 4). Key implementation notes:

    - Follow the exact urllib.request pattern from screenshot_agent_bridge.py — no aiohttp dependency in openclaw-fresh
    - SCHEDULER_URL = "http://openclaw-scheduler:9100"
    - `list_staging()`: GET /harness/sites?active_only=false, filter `s.get("active", True) == False` (not `not s.get("active", True)` — be explicit)
    - `promote_site(site_key)`: first calls list_staging() internally to get current site fields, then POSTs back with active=True; preserve category, priority, notes from original record
    - main() handles: `list`, `promote <site_key>`, and unknown/missing args (print error JSON, exit 0 not 1)
    - All error paths return {"ok": False, "error": str} — never raise to caller
    - Wrap URL errors with `except (urllib_error.URLError, urllib_error.HTTPError, Exception) as e`

    After writing, run promote_bridge unit tests: `cd /home/agent/openclaw-fresh/workspace/tools && python -m unittest test_promote_bridge -v`
    All tests must turn GREEN.

    Also do a live smoke test (if scheduler is reachable):
    ```bash
    docker exec openclaw-fresh python3 /var/lib/openclaw/workspace/tools/promote_bridge.py list
    ```
    Expected: JSON output with "ok": true and "staging" list (may be empty — that's fine).

    Commit atomically: `feat(03-03): add promote_bridge.py staging queue tool (GREEN)`
  </action>
  <verify>
    <automated>cd /home/agent/openclaw-fresh/workspace/tools && python -m unittest test_promote_bridge -v 2>&1 | tail -5</automated>
  </verify>
  <done>All tests in test_promote_bridge.py pass GREEN. promote_bridge.py handles list and promote subcommands. JSON output on stdout for all code paths.</done>
</task>

<task type="auto">
  <name>Task 2: Wire promote_bridge into workspace/TOOLS.md</name>
  <files>openclaw-fresh/workspace/TOOLS.md</files>
  <action>
    Append a new section to `/home/agent/openclaw-fresh/workspace/TOOLS.md` following the existing section style (see Gmail, gcal, notes sections).

    Add this section at the end of the file:

    ```markdown
    ## Perception Staging Queue

    When the user asks to list or promote staging URLs in the perception harness:
    - "list staging" / "show staging queue" / "what's in staging"
    - "promote <site_key>" / "add <site_key> to active tests"

    Use a single exec call to promote_bridge.py:

    List all staging entries (active=False):
    - `python3 /var/lib/openclaw/workspace/tools/promote_bridge.py list`
      Returns JSON: {"ok": true, "staging": [{site_key, base_url, ...}], "count": N}

    Promote a staging entry to active test suite:
    - `python3 /var/lib/openclaw/workspace/tools/promote_bridge.py promote <site_key>`
      Returns JSON: {"ok": true, "promoted": "<site_key>", "base_url": "..."}

    Failures return: {"ok": false, "error": "..."}

    UX rules:
    - If "ok": false, show the error message to the user and suggest checking site_key spelling.
    - After a successful promote, confirm with: "Promoted <site_key> (<base_url>) to active test suite."
    - If staging is empty, say: "Staging queue is empty — all sites are already active."
    - Do NOT edit config files directly. Use this tool.
    ```

    After editing, verify the file is readable without syntax issues:
    `head -5 /home/agent/openclaw-fresh/workspace/TOOLS.md` (should show the existing title)

    Commit atomically: `feat(03-03): wire promote_bridge into workspace TOOLS.md`
  </action>
  <verify>
    <automated>grep -n "promote_bridge" /home/agent/openclaw-fresh/workspace/TOOLS.md</automated>
  </verify>
  <done>TOOLS.md contains a "Perception Staging Queue" section with promote_bridge.py list and promote command examples. The section follows existing file style.</done>
</task>

</tasks>

<verification>
All promote_bridge tests pass: `cd /home/agent/openclaw-fresh/workspace/tools && python -m unittest test_promote_bridge -v 2>&1 | tail -3`

promote_bridge.py is executable from inside the container: `docker exec openclaw-fresh python3 /var/lib/openclaw/workspace/tools/promote_bridge.py list 2>&1 | head -3`

TOOLS.md contains the Perception Staging Queue section.
</verification>

<success_criteria>
- promote_bridge.py exists in openclaw-fresh/workspace/tools/ with list and promote subcommands
- All unit tests for promote_bridge pass GREEN
- workspace/TOOLS.md has Perception Staging Queue section with correct command examples
- Live smoke test from inside openclaw-fresh container returns valid JSON (ok: true or ok: false with error, not a crash)
</success_criteria>

<output>
After completion, create `.planning/phases/03-alerting-and-operator-workflow/03-03-SUMMARY.md`
</output>
