# Plan 04-01: Install tracker stack, verify imports, run 5-minute benchmark

**Phase:** 4 — Environment Setup & Benchmark
**Goal:** Install ultralytics/easyocr/torchreid into claude-stack-env, verify CPU inference, and benchmark the first 5 minutes of the Veo footage.

## Tasks

<wave id="1" label="Install dependencies">
  <task id="1.1" type="shell">
    Install ultralytics, easyocr, opencv-python into claude-stack-env
    Command: source /home/agent/claude-stack-env/bin/activate && pip install ultralytics easyocr opencv-python tqdm
  </task>
  <task id="1.2" type="shell">
    Install torchreid from GitHub (KaiyangZhou/deep-person-reid)
    Note: torchreid requires torch. Install torch CPU first, then torchreid.
    Command: source /home/agent/claude-stack-env/bin/activate && pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu && pip install torchreid
  </task>
</wave>

<wave id="2" label="Verify imports and weights">
  <task id="2.1" type="code">
    Write verify_imports.py to confirm all required packages import cleanly
    Path: /home/agent/verify_tracker_imports.py
  </task>
  <task id="2.2" type="shell">
    Run verify_tracker_imports.py — all imports must succeed
  </task>
</wave>

<wave id="3" label="Benchmark run">
  <task id="3.1" type="code">
    Write benchmark_tracker.py — runs YOLOv8 detection + OSNet on first 5 minutes, measures FPS, peak RAM, cosine sim distribution
    Path: /home/agent/benchmark_tracker.py
  </task>
  <task id="3.2" type="shell">
    Run benchmark_tracker.py on first 300 seconds of standard-1920x1080-followcam.mp4
    Output: /home/agent/veo-clips/player-continuity-tracking/benchmark_results.json
  </task>
</wave>

## Acceptance Criteria

1. `ultralytics`, `easyocr`, `torchreid` (or onnxruntime with osnet_ain_x1_0) all import without error in claude-stack-env
2. YOLOv8n.pt and osnet_ain_x1_0 weights download and run CPU inference
3. Benchmark run on first 5 minutes produces benchmark_results.json with fps, peak_ram_mb, cosine_sim_stats
