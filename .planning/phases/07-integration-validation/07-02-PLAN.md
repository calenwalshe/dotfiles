# Plan 07-02: 15-minute end-to-end test and eval plan execution

**Phase:** 7 — Integration & End-to-End Validation
**Goal:** Run 15-minute end-to-end pipeline; record eval plan results

## Tasks

<wave id="1" label="15-minute tracker run (can overlap with full video run)">
  <task id="1.1" type="shell">
    Run player_tracker.py for first 900s (15 min) on video
    Then gap_merge.py on that output
    Output: ~/veo-clips/player-continuity-tracking/player_ids_15min.json
  </task>
</wave>

<wave id="2" label="End-to-end clip extraction">
  <task id="2.1" type="shell">
    Run clip_extractor.py with 15-min tracker output
    Confirm clips written to output dir
  </task>
</wave>

<wave id="3" label="Eval plan execution">
  <task id="3.1" type="shell">
    Execute security check: grep for API key in outputs, verify File API empty
  </task>
  <task id="3.2" type="shell">
    Execute style check: grep for debug print statements in player_tracker.py
  </task>
  <task id="3.3" type="shell">
    Execute resilience tests: --test-frame black and blur
  </task>
  <task id="3.4" type="code">
    Record eval results in eval-plan.md
  </task>
</wave>

## Acceptance Criteria

1. 15-minute tracker output exists with annotations
2. clip_extractor.py runs to completion with tracker output (no crash)
3. Security check passes (no API key in output, File API empty after run)
4. Style check passes (no debug prints in production code)
5. Resilience tests pass (no exceptions on synthetic frames)
