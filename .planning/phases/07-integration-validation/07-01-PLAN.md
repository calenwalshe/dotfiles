# Plan 07-01: Integration shim in clip_extractor.py + regression test

**Phase:** 7 — Integration & End-to-End Validation
**Goal:** Minimal integration shim in clip_extractor.py; regression test with no tracker output

## Tasks

<wave id="1" label="Write integration shim">
  <task id="1.1" type="code">
    Modify ~/clip_extractor.py:
    - Add load_tracker_output(path) function → returns frame_segments list or None
    - Add --tracker-output optional arg
    - Add --no-tracker-output flag (explicit override)
    - Modify build_prompt() to accept tracker_context string parameter
    - Generate tracker_context from frame_segments (convert frames→seconds at fps 29.97)
    - Inject tracker_context into prompt when present
    - After Gemini extraction: delete File API upload immediately (verify with list_files)
  </task>
</wave>

<wave id="2" label="Regression test">
  <task id="2.1" type="shell">
    Verify clip_extractor.py --help shows new args without error
    Also dry-run import to confirm no syntax errors
  </task>
</wave>

## Acceptance Criteria

1. clip_extractor.py imports cleanly with no errors
2. --tracker-output arg accepted; --no-tracker-output accepted
3. With no tracker output (absent file): build_prompt returns same prompt as before (regression parity)
4. With tracker output: prompt includes frame range context
5. git diff shows minimal targeted changes to clip_extractor.py
