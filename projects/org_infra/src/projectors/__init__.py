"""Projectors — generate human-readable Markdown from the store.

Each projector reads from the Store API and writes Markdown files.
To add a new projection: write a new projector. No other component changes.
"""
