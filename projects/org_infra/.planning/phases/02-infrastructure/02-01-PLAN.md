---
phase: 02-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/graph/graph.py, src/graph/__init__.py, src/graph/state.py, tests/test_graph_routing.py]
autonomous: true
requirements: [ORCH-01]

must_haves:
  truths:
    - "Graph has 7 agent stub nodes + Orchestrator node + 4 gate nodes"
    - "Graph routes through Discovery → Definition → Pitch & Evaluation → Handoff"
    - "Gate nodes check artifact presence before advancing phase"
    - "Stub nodes produce minimal valid artifacts conforming to agent schemas"
  artifacts:
    - path: "src/graph/graph.py"
      provides: "LangGraph state machine definition"
      contains: "StateGraph"
    - path: "src/graph/state.py"
      provides: "Typed graph state"
      contains: "TypedDict"
    - path: "tests/test_graph_routing.py"
      provides: "Routing logic tests"
      min_lines: 40
  key_links:
    - from: "src/graph/graph.py"
      to: "src/graph/state.py"
      via: "Graph uses typed state"
      pattern: "GraphState"
---

<objective>
Implement the LangGraph graph skeleton with stub nodes for all agents and gate logic.

Purpose: This is the runtime backbone. Every agent plugs into this graph as a node. Gate nodes enforce the four-phase pipeline structure.
Output: `graph.py` (graph definition), `state.py` (typed state), routing tests
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/schemas/agent_artifacts.py
@src/schemas/handoff_package.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed graph state</name>
  <files>src/graph/__init__.py, src/graph/state.py</files>
  <action>
Create the graph state using TypedDict (LangGraph pattern):

```python
class GraphState(TypedDict):
    problem_statement: str
    current_phase: str  # "discovery" | "definition" | "pitch_evaluation" | "handoff"
    artifacts: dict[str, Any]  # agent_id -> serialized artifact
    gate_decisions: list[dict]  # history of gate decisions
    handoff_package: dict | None  # final output
    error: str | None
```

Create `src/graph/__init__.py` for package.
  </action>
  <verify>python -c "from src.graph.state import GraphState; print('OK')"</verify>
  <done>GraphState importable with all fields</done>
</task>

<task type="auto">
  <name>Task 2: Build graph skeleton with stub nodes and gates</name>
  <files>src/graph/graph.py</files>
  <action>
Build a LangGraph StateGraph with:

**Stub nodes (7 agents):**
- `orchestrator` — reads artifacts, returns gate decision (stub: always approves)
- `uxr` — returns minimal UXRArtifact
- `pm` — returns minimal PMArtifact
- `ds` — returns minimal DSArtifact
- `evaluation` — returns minimal EvaluationArtifact
- `pressure_test` — returns minimal PressureTestArtifact (with 1 objection)
- `feedback_synthesis` — returns minimal FeedbackSynthesisArtifact

**Phase routing:**
- Entry → orchestrator (intake) → discovery phase agents (uxr) → gate_1
- gate_1 → definition phase agents (pm, ds) → gate_2
- gate_2 → pitch_evaluation phase agents (evaluation, pressure_test, feedback_synthesis) → gate_3
- gate_3 → handoff (assembler stub) → END

**Gate nodes:**
Each gate checks that required artifacts exist in state before advancing. If missing, routes back to the phase agents.

Use `add_conditional_edges` for gate routing.

Export a `build_graph()` function that returns the compiled graph.
  </action>
  <verify>python -c "from src.graph.graph import build_graph; g = build_graph(); print('Graph built:', type(g))"</verify>
  <done>Graph builds without error, has all nodes and edges</done>
</task>

<task type="auto">
  <name>Task 3: Write routing logic tests</name>
  <files>tests/test_graph_routing.py</files>
  <action>
Write pytest tests for:
1. Graph builds successfully (no errors)
2. Graph runs end-to-end on synthetic input with stub nodes — produces a non-None handoff_package
3. Gate 1 passes when UXR artifact exists in state
4. Gate 2 passes when PM and DS artifacts exist
5. Gate 3 passes when Evaluation, PressureTest, and FeedbackSynthesis artifacts exist
6. All 4 phases are traversed in order (check gate_decisions length)

Use `graph.invoke()` for synchronous execution on synthetic problem statement.
  </action>
  <verify>cd /home/agent/projects/org_infra && python -m pytest tests/test_graph_routing.py -v</verify>
  <done>All routing tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_graph_routing.py -v` passes
- [ ] Graph has 7 agent nodes + gate nodes
- [ ] Stub nodes produce valid artifacts
- [ ] Full end-to-end run completes on synthetic input
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Graph structure matches the four-phase pipeline from spec
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure/02-01-SUMMARY.md`
</output>
