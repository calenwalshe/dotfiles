---
phase: 04-worker-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/agents/uxr.py, src/agents/pm.py, src/agents/ds.py, src/agents/evaluation.py, src/agents/pressure_test.py, src/agents/feedback_synthesis.py, src/integrations/comms.py, src/integrations/__init__.py]
autonomous: true
requirements: [AGENT-02, AGENT-03, AGENT-04, AGENT-05, AGENT-06, AGENT-07, AGENT-08]

must_haves:
  truths:
    - "All 6 agents produce typed artifacts conforming to their contract schemas"
    - "PM agent uses comms adapter for stakeholder communication"
    - "Pressure Testing produces specific named objections targeting pitch claims"
    - "Feedback Synthesis surfaces alignments and conflicts"
  artifacts:
    - path: "src/agents/uxr.py"
      provides: "UX Research agent"
    - path: "src/agents/pm.py"
      provides: "PM agent"
    - path: "src/agents/ds.py"
      provides: "Data Science agent"
    - path: "src/agents/evaluation.py"
      provides: "Evaluation agent"
    - path: "src/agents/pressure_test.py"
      provides: "Pressure Testing agent"
    - path: "src/agents/feedback_synthesis.py"
      provides: "Feedback Synthesis agent"
    - path: "src/integrations/comms.py"
      provides: "Comms adapter interface + mock"
---

<objective>
Implement all 6 specialist agents and the comms adapter.

Purpose: Replace stub nodes with real agent implementations that produce typed, quality-checked artifacts.
Output: 6 agent modules, comms adapter, integration with graph
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/schemas/agent_artifacts.py
@src/schemas/handoff_package.py
@docs/agent-contracts.md
@src/agents/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement comms adapter + all 6 agents</name>
  <files>src/integrations/__init__.py, src/integrations/comms.py, src/agents/uxr.py, src/agents/pm.py, src/agents/ds.py, src/agents/evaluation.py, src/agents/pressure_test.py, src/agents/feedback_synthesis.py</files>
  <action>
Implement all agents following the same pattern: class with run() method returning typed artifact.

Each agent is rule-based/template-based (no LLM calls). Takes relevant inputs from graph state and produces its typed artifact.
  </action>
  <verify>python -c "from src.agents import uxr, pm, ds, evaluation, pressure_test, feedback_synthesis; print('OK')"</verify>
  <done>All 6 agents importable</done>
</task>

<task type="auto">
  <name>Task 2: Write agent tests</name>
  <files>tests/test_worker_agents.py</files>
  <action>
Write tests for all 6 agents verifying they produce valid typed artifacts.
  </action>
  <verify>cd /home/agent/projects/org_infra && python -m pytest tests/test_worker_agents.py -v</verify>
  <done>All agent tests pass</done>
</task>

</tasks>

<verification>
- [ ] All 6 agents produce valid artifacts
- [ ] PM agent uses comms adapter
- [ ] Full test suite passes
</verification>

<success_criteria>
- All 6 agents implemented and tested
- Artifacts pass Orchestrator quality checks
</success_criteria>

<output>
After completion, create `.planning/phases/04-worker-agents/04-01-SUMMARY.md`
</output>
