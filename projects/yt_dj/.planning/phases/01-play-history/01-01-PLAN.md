---
phase: 01-play-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/app.py
  - src/analyze_library.py
autonomous: true
must_haves:
  truths:
    - "music/library.db contains library_tracks and plays tables with the exact schema from spec Section 4"
    - "PRAGMA journal_mode on library.db returns wal"
    - "After running python3 src/analyze_library.py, SELECT COUNT(*) FROM library_tracks WHERE removed_at IS NULL equals ls music/clips/*.mp3 | wc -l"
  artifacts:
    - path: "src/web/app.py"
      provides: "get_db() creates library_tracks + plays tables + WAL mode"
      contains: "library_tracks"
    - path: "src/analyze_library.py"
      provides: "upsert_library_tracks() populates library_tracks from music/clips/"
      contains: "upsert_library_tracks"
  key_links:
    - from: "src/web/app.py get_db()"
      to: "music/library.db"
      via: "CREATE TABLE IF NOT EXISTS library_tracks + plays + PRAGMA journal_mode=WAL"
      pattern: "library_tracks"
    - from: "src/analyze_library.py upsert_library_tracks()"
      to: "music/library.db library_tracks"
      via: "INSERT OR IGNORE / ON CONFLICT UPDATE"
      pattern: "upsert_library_tracks"
---

<objective>
Create the SQLite schema foundation: extend get_db() in src/web/app.py with the two new tables (library_tracks and plays), all indexes, and WAL mode. Then extend src/analyze_library.py with upsert_library_tracks() so running the script populates library_tracks with one row per music/clips/*.mp3 file.

Purpose: Establishes the durable storage layer that everything else (writer, read API) depends on. Must be in place before Plan 02 adds the read API.

Output: library_tracks and plays tables in music/library.db, WAL mode enabled, library_tracks populated after first analyze_library.py run.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-play-history/01-CONTEXT.md
@src/web/app.py
@src/analyze_library.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend get_db() in src/web/app.py with library_tracks, plays, indexes, and WAL mode</name>
  <files>src/web/app.py</files>
  <action>
    In src/web/app.py, find the get_db() function (currently around line 152). After the existing CREATE TABLE IF NOT EXISTS tracks (...) statement and its conn.commit(), add:

    1. PRAGMA journal_mode=WAL:
       conn.execute("PRAGMA journal_mode=WAL")

    2. CREATE TABLE IF NOT EXISTS library_tracks:
       conn.execute("""
           CREATE TABLE IF NOT EXISTS library_tracks (
               id            INTEGER PRIMARY KEY AUTOINCREMENT,
               file_path     TEXT    NOT NULL UNIQUE,
               filename      TEXT    NOT NULL,
               title         TEXT,
               artist        TEXT,
               bpm           REAL,
               camelot       TEXT,
               duration_s    REAL,
               first_seen_at TEXT    NOT NULL,
               removed_at    TEXT
           )
       """)

    3. CREATE TABLE IF NOT EXISTS plays:
       conn.execute("""
           CREATE TABLE IF NOT EXISTS plays (
               id               INTEGER PRIMARY KEY AUTOINCREMENT,
               library_track_id INTEGER,
               file_path        TEXT    NOT NULL,
               filename         TEXT    NOT NULL,
               played_at        TEXT    NOT NULL,
               source           TEXT    DEFAULT 'liquidsoap'
           )
       """)

    4. All indexes (run as separate conn.execute() calls):
       conn.execute("CREATE INDEX IF NOT EXISTS idx_library_tracks_file_path ON library_tracks(file_path)")
       conn.execute("CREATE INDEX IF NOT EXISTS idx_library_tracks_removed_at ON library_tracks(removed_at)")
       conn.execute("CREATE INDEX IF NOT EXISTS idx_plays_library_track_id ON plays(library_track_id, played_at DESC)")
       conn.execute("CREATE INDEX IF NOT EXISTS idx_plays_played_at ON plays(played_at DESC)")
       conn.execute("CREATE INDEX IF NOT EXISTS idx_plays_file_path ON plays(file_path)")

    5. One final conn.commit() after all the above.

    IMPORTANT: Do NOT change the existing tracks table definition or any existing code. Only add new CREATE TABLE and CREATE INDEX statements, the WAL PRAGMA, and the commit. The existing conn.commit() after CREATE TABLE tracks can stay in place — we're adding a second pass with the new tables.

    Do NOT add a DB_PATH import — it's already defined at line ~33 as DB_PATH = PROJECT_DIR / "music" / "library.db".

    After editing, restart yt-dj-web.service:
    sudo systemctl restart yt-dj-web.service
  </action>
  <verify>
    sqlite3 /home/agent/projects/yt_dj/music/library.db ".schema library_tracks" | grep -c "INTEGER PRIMARY KEY"
    # Expected: 1 (table exists with correct structure)
    
    sqlite3 /home/agent/projects/yt_dj/music/library.db ".schema plays" | grep -c "INTEGER PRIMARY KEY"
    # Expected: 1
    
    sqlite3 /home/agent/projects/yt_dj/music/library.db "PRAGMA journal_mode"
    # Expected: wal
    
    curl -s http://localhost:9093/api/tracks | python3 -c "import sys,json; d=json.load(sys.stdin); print('OK' if isinstance(d, list) else 'FAIL')"
    # Expected: OK (regression check — existing endpoint still works)
  </verify>
  <done>library_tracks and plays tables exist in library.db; PRAGMA journal_mode returns wal; existing /api/tracks endpoint still returns a list.</done>
</task>

<task type="auto">
  <name>Task 2: Extend src/analyze_library.py with upsert_library_tracks()</name>
  <files>src/analyze_library.py</files>
  <action>
    In src/analyze_library.py, add the following changes:

    1. Add imports at the top (after the existing imports):
       import sqlite3
       from datetime import datetime, timezone
       
       (Note: sqlite3 may already be imported; check first and skip if so)

    2. Add DB_PATH constant after the existing OUTPUT constant:
       DB_PATH = PROJECT / "music" / "library.db"

    3. Add the upsert_library_tracks() function after the analyze_track() function (before main()):

       def upsert_library_tracks(results: list) -> None:
           """Upsert library_tracks table from analysis results. Soft-deletes removed files."""
           now = datetime.now(timezone.utc).isoformat(timespec="seconds")
           conn = sqlite3.connect(str(DB_PATH))
           conn.execute("PRAGMA journal_mode=WAL")
           try:
               for r in results:
                   conn.execute(
                       """
                       INSERT INTO library_tracks
                           (file_path, filename, bpm, camelot, duration_s, first_seen_at)
                       VALUES (?, ?, ?, ?, ?, ?)
                       ON CONFLICT(file_path) DO UPDATE SET
                           filename   = excluded.filename,
                           bpm        = excluded.bpm,
                           camelot    = excluded.camelot,
                           duration_s = excluded.duration_s,
                           removed_at = NULL
                       """,
                       (r["file"], r["filename"], r.get("bpm"), r.get("camelot"), r.get("duration_s"), now)
                   )
               # Soft-delete files no longer in the scan
               current_paths = {r["file"] for r in results}
               rows = conn.execute(
                   "SELECT file_path FROM library_tracks WHERE removed_at IS NULL"
               ).fetchall()
               for (fp,) in rows:
                   if fp not in current_paths:
                       conn.execute(
                           "UPDATE library_tracks SET removed_at = ? WHERE file_path = ?",
                           (now, fp)
                       )
               conn.commit()
               log.info(f"Upserted {len(results)} tracks into library_tracks")
           finally:
               conn.close()

    4. In main(), add a call to upsert_library_tracks(results) AFTER the JSON write (after the line "with open(OUTPUT, 'w') as f: json.dump(results, f, indent=2)"):
       upsert_library_tracks(results)

    IMPORTANT: The ON CONFLICT clause leaves first_seen_at alone on update (it is not in the UPDATE SET list). This is correct — first_seen_at is set only on initial insert.

    The "file" key in results is the full absolute path (r["file"]). The "filename" key is the basename. Both already exist in the dict returned by analyze_track().
  </action>
  <verify>
    python3 /home/agent/projects/yt_dj/src/analyze_library.py 2>&1 | tail -5
    # Should show "Upserted N tracks into library_tracks" at the end
    
    CLIP_COUNT=$(ls /home/agent/projects/yt_dj/music/clips/*.mp3 2>/dev/null | wc -l)
    DB_COUNT=$(sqlite3 /home/agent/projects/yt_dj/music/library.db "SELECT COUNT(*) FROM library_tracks WHERE removed_at IS NULL")
    echo "clips=$CLIP_COUNT db=$DB_COUNT"
    # Expected: both numbers match (or DB >= clips if library/ dir also has mp3s)
    
    sqlite3 /home/agent/projects/yt_dj/music/library.db "SELECT filename, bpm, camelot, first_seen_at FROM library_tracks LIMIT 3"
    # Should show rows with filename, bpm/camelot values, and ISO timestamp
  </verify>
  <done>library_tracks has one row per file in music/clips/ (and music/library/ if it has files); first_seen_at is populated for all rows; removed_at is NULL for all current files.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] sqlite3 library.db ".schema library_tracks" shows correct DDL
- [ ] sqlite3 library.db ".schema plays" shows correct DDL
- [ ] sqlite3 library.db "PRAGMA journal_mode" returns "wal"
- [ ] python3 src/analyze_library.py runs without error and logs "Upserted N tracks"
- [ ] SELECT COUNT(*) FROM library_tracks WHERE removed_at IS NULL matches ls music/clips/*.mp3 | wc -l
- [ ] curl http://localhost:9093/api/tracks returns valid JSON list (regression check)
</verification>

<success_criteria>
- All tasks completed
- Both tables exist with correct schema
- WAL mode active
- library_tracks populated from music/clips/
- No regression on existing endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/01-play-history/01-01-SUMMARY.md`
</output>
