---
plan: 01-01
phase: 1
wave: 1
autonomous: true
---

# Plan 01-01: SSH Hardening

## Objective

Harden SSH config: disable PasswordAuthentication and PermitRootLogin (key auth confirmed — 7 authorized_keys entries), disable X11Forwarding and GatewayPorts, set LogLevel VERBOSE. Document port 2022 as etserver (blocked by UFW default deny — no action needed). Restart sshd and verify.

## Tasks

- [ ] Document port 2022 (etserver — EternalTerminal, UFW blocks externally, no change needed)
- [ ] Set `PasswordAuthentication no` in `/etc/ssh/sshd_config.d/50-cloud-init.conf`
- [ ] Set `PermitRootLogin no`, `X11Forwarding no`, `GatewayPorts no`, `LogLevel VERBOSE` in `/etc/ssh/sshd_config`
- [ ] `sudo systemctl restart sshd`
- [ ] Verify: `sshd -T | grep -E "passwordauthentication|permitrootlogin|x11forwarding|gatewayports|loglevel"`

## Success Criteria

- `sshd -T` returns `passwordauthentication no` and `permitrootlogin no`
- `sshd -T` returns `x11forwarding no`, `gatewayports no`, `loglevel VERBOSE`
