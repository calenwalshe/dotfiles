---
plan: 02-01
phase: 2
wave: 1
autonomous: true
---

# Plan 02-01: Docker Port Rebinding

## Objective

Rebind all internal Docker service ports from bare `PORT:PORT` to `127.0.0.1:PORT:PORT` in `/home/agent/agent-stack/docker-compose.yml`. Keep Caddy's `443:443` and `80:80` on 0.0.0.0. Ports already bound to 127.0.0.1 (5232, 3900, 3903) stay as-is. Restart affected services. Verify with nmap.

## Ports to rebind

These go from `"PORT:PORT"` → `"127.0.0.1:PORT:PORT"`:
- `${CODEX_BRIDGE_PORT:-9090}:9090`
- `${SMART_ROUTER_PORT:-9080}:9080`
- `${GEMINI_BRIDGE_PORT:-9091}:9091`
- `9099:9099`, `9095:9095`, `3003:3003`, `9200:9200`, `9120:9120`
- `9600:9600`, `9098:9098`, `9310:9310`, `9500:9500`
- `9100:9100`, `9110:9110`, `9130:9130`, `9140:9140`, `9150:9150`, `9260:9260`

Leave untouched: `443:443`, `80:80`, `127.0.0.1:5232:5232`, `127.0.0.1:3900:3900`, `127.0.0.1:3903:3903`

## Tasks

- [ ] Backup docker-compose.yml
- [ ] Apply sed substitutions for all internal ports
- [ ] Verify Caddy ports unchanged, all others use 127.0.0.1
- [ ] `docker compose -f /home/agent/agent-stack/docker-compose.yml up -d`
- [ ] Verify `docker ps --format "{{.Ports}}"` — no 0.0.0.0:PORT except Caddy 80/443
- [ ] `nmap -p 9090,9091,9095,9098,9099 149.28.12.120` — confirm filtered/closed

## Success Criteria

- `docker ps` shows no `0.0.0.0:PORT` bindings except Caddy 80 and 443
- nmap shows all listed ports filtered or closed
