---
plan: 03-01
phase: 3
wave: 1
autonomous: true
---

# Plan 03-01: Caddy Security Headers + Dashboard Auth + VNC Cleanup

## Objective

Add global `(security_headers)` snippet to Caddyfile, import it in all vhosts. Add basicauth to /status and /costs routes. Remove the stale vnc-auth.calenwalshe.com block. Validate and restart Caddy.

## Caddyfile location

`/home/agent/agent-stack/caddy/Caddyfile`

## Tasks

- [ ] Generate bcrypt password hash for dashboard basicauth
- [ ] Add `(security_headers)` snippet at top of Caddyfile
- [ ] Import `security_headers` in each vhost block
- [ ] Add basicauth to /status and /costs handlers in :443 block
- [ ] Remove vnc-auth.calenwalshe.com block (lines ~140-151)
- [ ] Validate: `docker exec caddy caddy validate --config /etc/caddy/Caddyfile`
- [ ] Restart: `cd /home/agent/agent-stack && docker compose restart caddy`
- [ ] Verify headers: `curl -sI https://radio.calenwalshe.com`
- [ ] Verify dashboard auth: `curl -s -o /dev/null -w "%{http_code}" https://radio.calenwalshe.com/status`

## Success Criteria

- `curl -sI https://radio.calenwalshe.com` shows Strict-Transport-Security, X-Content-Type-Options, X-Frame-Options, Referrer-Policy; no Server: header
- `/status` returns 401 for unauthenticated request
- `grep -c "vnc-auth.calenwalshe.com" Caddyfile` = 0
